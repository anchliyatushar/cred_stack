import 'package:flutter/material.dart';

class EmiPlanWidget extends StatelessWidget {
  const EmiPlanWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('EmiPlan'),
      ),
    );
  }
}
